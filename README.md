# Velocitrack_Dev

File descriptions:

```
study_cam_notstatic.py # run client to collect data from high-speed camera
wall_saver_study.py # run server to receive data from cameras and visualize. Also tracks the hand and visualizes output
data_viewer.py # can be used to view saved recordings
train.py # used to train an ML model for the standard and adversarial anlyses
train_ablation.py # used to train an ML model for the tap intensity and frame rate ablations
train_acc.py # used to train an ML model specifically for the IMU baseline
```